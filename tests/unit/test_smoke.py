"""Smoke test placeholder — ensures pytest collects at least one test before
real test files land in subsequent tasks. Remove once any other test exists.
"""

from __future__ import annotations


def test_pytest_runs() -> None:
    assert True
